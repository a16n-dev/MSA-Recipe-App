export type recipe = {
    _id: string
    name: string
    ingredients: string[]
    method: string[]
    notes: string
}

export type user = {
    name: String
    photoUrl: String
    email: String
}